# 曦望炒股一体机 · 隐私确认闸门 —— 项目工作总结

> 一份面向产品与商业价值的工作总结，重点阐述本项目在**隐私保护**与**本地推理**上的设计与实现，以支撑本地推理硬件的销售定位。

---

## 一、项目背景与目标

### 1.1 我们在卖什么

我们卖的是**本地推理硬件（炒股一体机）**。它的核心卖点不是"能跑 AI"——云端 AI 谁都能用——而是：**让用户在享受完整 AI 分析能力的同时，最敏感的个人金融数据永远不离开自己的机器。**

### 1.2 为什么金融场景特别需要本地推理

散户投资者最不愿意暴露的两类数据：

- **持仓数量（qty）** —— 暴露了你的仓位规模、资金体量
- **成本价（cost）** —— 暴露了你的盈亏状态、心理防线、操作空间

一旦这些数据进入云端 AI 服务商的服务器，就意味着：可能被记录、被用于训练、被关联画像、被泄露。对于把身家押在股市里的用户，这是不可接受的。

**这正是本地推理硬件的商业机会**：把"AI 分析"和"数据主权"这两件本来矛盾的事，用一台本地机器同时解决。

### 1.3 本项目的定位

本项目（代号 Sunrise / 隐私确认闸门）是一个**可运行的产品演示（Demo）**，用来向客户、领导、投资人**直观证明这套隐私架构是真实可行的，不是营销话术**。它把"敏感数据不出本地"从一句承诺，变成了一条看得见、可审计的数据流。

---

## 二、核心设计理念：隐私确认闸门

整个产品围绕一个核心机制展开——**隐私确认闸门（Privacy Confirmation Gate）**。

传统 AI 炒股工具的流程是：`用户数据 → 云端 AI → 返回结果`，数据一股脑全上传。

我们的流程是：

```
本地分析 → 【闸门】用户亲眼确认要发什么 → 云端只搜公开信息 → 本地 AI 结合个人数据综合
```

关键在于中间这道**闸门**：在任何数据离开本地之前，系统会暂停，把"即将发往云端的全部内容"原样摊开给用户看，用户点了确认才继续。用户看到的，就是发出去的——不多一个字节。

这道闸门在技术上由 **LangGraph 的 `interrupt` 机制**实现：分析流程执行到确认节点时真正挂起，等待用户决策后才恢复，而不是前端做做样子。

---

## 三、系统架构总览

我设计并实现了一套**双 Agent + 本地融合**的三段式架构：

```
┌─────────────────── 本地（用户的硬件）───────────────────┐
│                                                          │
│  Agent A：本地分析                                        │
│   · 读取本地持仓文件（含持仓量、成本）                     │
│   · 拉取行情、计算技术指标                                 │
│   · 生成脱敏研究问题                                       │
│                     │                                    │
│              【隐私确认闸门】← 用户确认                    │
│                     │                                    │
└─────────────────────┼────────────────────────────────────┘
                      │  只发送：股票代码 + 脱敏问题
                      ▼
┌─────────────────── 云端 ─────────────────────────────────┐
│  Agent B：云端调研                                        │
│   · 只搜索公开新闻、公告、研报                             │
│   · 完全不接触持仓量和成本                                 │
└─────────────────────┼────────────────────────────────────┘
                      │  只返回：公开信息
                      ▼
┌─────────────────── 本地（用户的硬件）───────────────────┐
│  Fusion：本地 AI 融合分析                                 │
│   · 云端新闻 + 本地技术信号 + 个人持仓成本                 │
│   · 由本地 AI 给出结合盈亏的个性化建议                     │
└──────────────────────────────────────────────────────────┘
```

**数据流的三条铁律：**

1. 敏感数据（qty/cost）在 Agent A 本地产生，本地使用
2. 发往云端的对象里**从结构上就不存在**这两个字段
3. 最终的个性化分析（Fusion）回到本地进行，个人数据与 AI 在同一台机器上相遇

---

## 四、我完成的具体工作

### 4.1 持仓数据管理

- 设计了本地持仓文件 `holdings.json`，字段包括：股票代码、名称、**持仓量、成本价**、行业标签、**风险偏好、持有周期、投资备注**
- 引入整体组合的 `risk_profile`（风险偏好）
- 实现从文件动态读取，替代硬编码，用户可自行维护自己的持仓

### 4.2 Agent A —— 本地全方位分析

**实时估值（腾讯财经 CDN）**
- 零注册接口拉取实时价、PE(TTM)、PB、总市值

**历史行情（BaoStock 私有 socket 协议）**
- 拉取 90 天日线数据
- 关键：走私有 socket，不依赖易被封锁的 HTTP 接口，云端服务器上也稳定

**技术指标本地计算（pandas）**
- 均线金叉/死叉（MA5/MA20）
- RSI(14) 超买超卖
- MACD(12/26/9) 金叉死叉
- KDJ(9) 
- 换手率与量能趋势
- 所有指标在本地完成，不依赖任何外部计算服务

**智能研究问题生成**
- 调用大模型，根据技术信号 + 风险偏好 + 持有周期 + 投资备注，生成针对性研究问题
- 问题被自动分类（公告类 / 新闻类 / 分析类），决定后续调研策略

### 4.3 隐私确认闸门

- 基于 LangGraph `interrupt` 实现真正的流程挂起
- 前端可折叠/展开的确认界面，逐股展示技术信号与待发问题
- **确认清单严格等于发送内容**：界面上不显示任何持仓量、成本，避免"我看到了是不是也发出去了"的误会
- 用户可一键确认，或取消（取消则完全不联网，只看本地结果）

### 4.4 Agent B —— 云端调研（数据最小化）

**两层信源策略，最大化信噪比：**

- 第一层 · 结构化数据（主力）：个股新闻、题材归因、分析师一致预期 EPS
- 第二层 · 网络搜索（补充）：只对"分析类"问题启用，且带**域名白名单**过滤噪声，再经**大模型二次过滤**剔除无关内容

**隐私约束（核心）：**
- Agent B 只接收 `股票代码 + 脱敏问题`
- 搜索请求里从不拼接持仓量、成本
- 从代码结构上保证：发往云端的 `research_questions` 对象里根本没有这些字段

### 4.5 Fusion —— 本地 AI 融合分析

- 把三方信息汇总：云端返回的公开新闻 + 本地技术信号 + **本地个人持仓成本**
- 本地计算浮盈/浮亏比例与金额
- 由 AI 给出**结合盈亏状态的个性化建议**（浮亏是否补仓、浮盈是否止盈）
- **定位明确**：这是本地步骤。个人数据只在这里、只在本地与 AI 相遇

### 4.6 前后端实时体验

- 全程 SSE（Server-Sent Events）实时推送每一步进度
- 序列图式界面：本地列（青色）/ 云端列（琥珀色）泾渭分明，用户能**肉眼看清哪些计算在本地、哪些在云端**
- 综合建议用 Markdown 渲染，标题、加粗、列表正确排版
- 选股面板：支持单只分析或多只联合分析

### 4.7 工程与部署

- FastAPI + uvicorn 服务化
- LangGraph 状态机编排、msgpack 序列化的类型安全处理
- 代理环境隔离，保证云端服务器网络稳定
- 完成云平台部署（Railway），配置化的密钥管理

---

## 五、隐私保护特点总结（本项目的灵魂）

| 数据类型 | 本地分析 | 发往云端 | 本地 AI 分析 |
|----------|:---:|:---:|:---:|
| 股票代码 / 名称 | ✅ | ✅ | ✅ |
| **持仓量（qty）** | ✅ | **❌ 从不** | ✅ |
| **成本价（cost）** | ✅ | **❌ 从不** | ✅ |
| 技术指标信号 | ✅ | ❌ | ✅ |
| 研究问题（脱敏） | 本地生成 | ✅ | ✅ |
| 风险偏好 / 备注 | ✅ | ❌ | ✅ |

**四个可审计的隐私保障：**

1. **结构性隔离**：发往云端的数据对象里，敏感字段在代码层面就不存在，不是"记得别传"，而是"根本没有这条传输路径"
2. **用户知情确认**：闸门把发送内容 100% 摊开，用户看到什么就发送什么
3. **数据最小化**：云端只拿到搜公开新闻所必需的最少信息
4. **本地融合**：需要结合个人数据的那一步分析，物理上发生在本地

---

## 六、为什么这一切都指向"本地推理硬件"

前面所有设计，最终收敛到一个价值主张：

> **只有当最终的 AI 推理在本地进行，"隐私"才是完整的。**

**云端 AI 的根本困境**：要做出真正个性化的建议（"你成本 128，现价 74，浮亏 42%，该不该补仓"），AI 就必须知道你的持仓成本。可一旦知道，数据就上了云。个性化与隐私，在云端架构下是**不可调和**的矛盾。

**本地推理硬件是唯一的解**：

- 云端 AI 负责它该负责的——搜集**公开信息**（新闻、公告、研报），这些本来就是公开的，上不上云无所谓
- 本地 AI 负责它该负责的——把公开信息与**你的私人数据**结合，做出只属于你的判断
- 私人数据与 AI 的"相遇"，发生在你手里的那台机器上，一个字节都不外流

**本项目就是这个价值主张的活体证明。** 当前 Demo 中，融合分析这一步临时调用了云端 API（因为本地模型尚在集成），但整套架构已经为本地模型预留好了位置——**换上本地推理芯片，个人数据就实现了 100% 不出本地的闭环**。届时：

- 用户得到的分析质量，与用云端大模型**没有区别**
- 但用户的持仓、成本、盈亏，**没有任何第三方能看到**

这就是我们的硬件要卖的东西：**不牺牲 AI 能力的数据主权。**

---

## 七、结语

我在本项目中完成的，不只是一个炒股分析工具，而是一套**可以直接向客户演示、经得起技术审计的隐私架构范式**。它把"本地推理硬件为什么值得买"这个抽象的商业主张，变成了一条用户能亲眼看见、工程师能逐行验证的数据流。

对潜在客户，它回答了那个最关键的问题：**"我的钱袋子，凭什么交给一台机器？"** —— 因为在这台机器上，AI 为你服务，但你的秘密只有你自己知道。
