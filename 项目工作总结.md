# 曦望炒股一体机 · 隐私确认闸门 —— 项目工作总结

> 一份面向产品与商业价值的工作总结，重点阐述本项目在**隐私保护**与**本地推理**上的设计与实现，以支撑本地推理硬件的销售定位。

---

## 一、项目背景与目标

### 1.1 我们在卖什么

我们卖的是**本地推理硬件（炒股一体机）**。它的核心卖点不是"能跑 AI"——云端 AI 谁都能用——而是：**让用户在享受完整 AI 分析能力的同时，最敏感的个人金融数据永远不离开自己的机器。**

### 1.2 为什么金融场景特别需要本地推理

散户投资者最不愿意暴露的两类数据：

- **持仓数量（qty）** —— 暴露了你的仓位规模、资金体量
- **成本价（cost）** —— 暴露了你的盈亏状态、心理防线、操作空间

一旦这些数据进入云端 AI 服务商的服务器，就意味着：可能被记录、被用于训练、被关联画像、被泄露。对于把身家押在股市里的用户，这是不可接受的。

**这正是本地推理硬件的商业机会**：把"AI 分析"和"数据主权"这两件本来矛盾的事，用一台本地机器同时解决。

### 1.3 本项目的定位

本项目（代号 Sunrise / 隐私确认闸门）是一个**可运行的产品演示（Demo）**，用来向客户、领导、投资人**直观证明这套隐私架构是真实可行的，不是营销话术**。它把"敏感数据不出本地"从一句承诺，变成了一条看得见、可审计的数据流。

---

## 二、核心设计理念：隐私确认闸门

整个产品围绕一个核心机制展开——**隐私确认闸门（Privacy Confirmation Gate）**。

传统 AI 炒股工具的流程是：`用户数据 → 云端 AI → 返回结果`，数据一股脑全上传。

我们的流程是：

```
本地分析 → 【闸门】用户亲眼确认要发什么 → 云端只搜公开信息 → 本地 AI 结合个人数据综合
```

关键在于中间这道**闸门**：在任何数据离开本地之前，系统会暂停，把"即将发往云端的全部内容"原样摊开给用户看，用户点了确认才继续。用户看到的，就是发出去的——不多一个字节。

这道闸门不是前端做个弹窗装装样子——它是分析流程本身真实地被冻结在原地：所有中间状态（已经跑完的本地结果、生成好的问题清单）被完整保存下来，流程真正停摆，等待用户的决定，决定做出之后才从原地继续往下走，而不是重新跑一遍。这一点在下面"实现细节"里会具体说明是怎么做到的。

---

## 三、系统架构总览

我设计并实现了一套**双 Agent + 本地融合**的三段式架构：

```
┌─────────────────── 本地（用户的硬件）───────────────────┐
│                                                          │
│  Agent A：本地分析                                        │
│   · 读取本地持仓文件（含持仓量、成本）                     │
│   · 拉取行情、计算技术指标                                 │
│   · 生成脱敏研究问题                                       │
│                     │                                    │
│              【隐私确认闸门】← 用户确认                    │
│                     │                                    │
└─────────────────────┼────────────────────────────────────┘
                      │  只发送：股票代码 + 脱敏问题
                      ▼
┌─────────────────── 云端 ─────────────────────────────────┐
│  Agent B：云端调研                                        │
│   · 只搜索公开新闻、公告、研报                             │
│   · 完全不接触持仓量和成本                                 │
└─────────────────────┼────────────────────────────────────┘
                      │  只返回：公开信息
                      ▼
┌─────────────────── 本地（用户的硬件）───────────────────┐
│  Fusion：本地 AI 融合分析                                 │
│   · 云端新闻 + 本地技术信号 + 个人持仓成本                 │
│   · 由本地 AI 给出结合盈亏的个性化建议                     │
└──────────────────────────────────────────────────────────┘
```

**数据流的三条铁律：**

1. 敏感数据（qty/cost）在 Agent A 本地产生，本地使用
2. 发往云端的对象里**从结构上就不存在**这两个字段
3. 最终的个性化分析（Fusion）回到本地进行，个人数据与 AI 在同一台机器上相遇

**架构上还有一个不太显眼、但很关键的设计**：整个流程被拆成了两段独立的实时通道，中间用一个"断点"接起来——本地分析阶段一条通道，用户确认之后云端调研+融合阶段再开一条新通道。这不是随意的技术选型，而是因为中间那道闸门等待用户点击确认，这个等待时间是不确定的（可能几秒，也可能用户去倒杯水回来点），没法用一条永远开着的连接干等。这套"分阶段通道 + 断点续传"的设计，后面会展开讲。

---

## 四、我完成的具体工作

### 4.1 持仓数据管理：不只是"一份清单"，而是"个性化的输入源"

我设计了本地持仓文件，作为整个系统唯一的个人数据来源，里面每一支股票记录的不只是"代码和名称"，而是六类信息：

- **持仓量、成本价** —— 最敏感的两个字段，决定了浮盈浮亏
- **行业标签** —— 用于展示归类，帮助用户快速识别
- **风险偏好**（低/中/高） —— 这支股票在用户心目中的角色是"防御仓"还是"弹性仓"
- **持有周期**（短/中/长线） —— 决定了后面研究问题该聚焦"催化剂"还是"基本面"
- **投资备注** —— 用户自己写的一句话逻辑（比如"核心仓位，看好国产化替代"），会被原样喂给 AI

另外整个持仓组合还有一个**整体风险偏好**（如"稳健型"），代表用户的总体投资风格。

这样设计的意图是：**持仓文件不只是给系统看行情用的，它本质上是一份"用户画像"**，风险偏好、持有周期、个人逻辑这些主观信息全部结构化存下来，后面每一步分析——无论是生成研究问题、还是最终给建议——都会读取这些字段，让 AI 的产出天然带上"这是关于你、为你定制"的味道，而不是一份放之四海皆准的通用报告。而且文件是用户自己可编辑的普通配置文件，不需要碰代码就能维护自己的持仓和想法。

### 4.2 Agent A —— 本地全方位分析

这一步的目标是：**在联网之前，先把能在本地算清楚的东西全部算清楚**，联网调研应该去补什么、缺什么，交给下一步的问题生成来判断，而不是本地和云端各干各的、互相不知道对方在干嘛。

**实时估值**
用一个零注册、免费的行情接口拿到当前股价、市盈率（PE）、市净率（PB）、总市值。这一路的作用是"先垫底"——保证哪怕后面更精细的数据源失败，用户也至少能看到一个大致靠谱的价格和估值。

**历史行情（走私有协议而非普通网页接口）**
拉取近 90 天的日线数据。这里刻意选择了一个**走私有 socket 协议、不依赖普通网页抓取**的数据源，原因是这类专业行情接口通常比"扒财经网站网页"更稳定、限制更少，尤其是部署到云服务器上跑的时候，不容易被当成爬虫封锁。拿到的市盈率、市净率数据也比第一步的估算值更精确，所以会用这一路的结果**覆盖**掉前一步的估算值——两路数据不是简单二选一，而是"快但粗"的兜底加上"慢但准"的精修。

**技术指标本地计算**
所有指标——均线金叉死叉、RSI 超买超卖、MACD 金叉死叉、KDJ、换手率与量能变化趋势——全部在本地用历史数据现算，不依赖任何外部计算服务或收费指标接口。选择这五组指标是因为它们分别从不同角度回答"这支股票现在处于什么状态"：均线看趋势方向、量能看资金参与度、RSI/KDJ 看短期是否超买超卖、MACD 看动能变化、换手率看情绪热度。五个信号放在一起看，比单独看任何一个都更立体，这也是后面生成研究问题时的核心依据。

**统一的容错设计**
估值拉取、历史行情拉取这两路是完全独立的两套 try/except，任何一路失败都不会拖累另一路，也不会让整个分析中断——历史行情失败时会自动切换到一份"标注为模拟数据"的兜底结果，保证流程能继续走完，同时前端会如实告诉用户"这里用的是模拟数据"，不会用假数据冒充真实结果。这个"每个外部依赖独立兜底、失败要显式告知而不是悄悄掩盖"的原则，贯穿了整个项目所有对接外部服务的地方。

### 4.3 智能研究问题生成：把"个性化"提前到发送云端之前

这是整个架构里我认为**最关键的一处结构性设计**：个性化本应该发生在拿到云端信息之后的"综合建议"阶段，但如果只有那一步做个性化，之前发给云端的问题就会是一份千篇一律的通用问题清单，浪费了云端调研的针对性。

我的做法是：把本地算出来的技术信号，连同这支股票的**风险偏好、持有周期、个人备注**，一起交给大模型，让它生成 2~3 个"需要联网查证据"才能回答的研究问题——短线仓位问题会侧重"最近有没有催化剂事件"，长线仓位会侧重"基本面有没有变化"，高风险仓位会更关注"下行风险"。也就是说，**发往云端的问题本身，就已经是为这个用户、这笔仓位量身定制的**，而不是一个通用的"这支股票怎么样"。

同时明确要求 AI 生成问题时**绝不能提及持仓数量和成本价**——个性化只体现在"问什么"上，不体现在"问题里包含什么数据"上，这是把个性化和隐私脱钩的关键一步：懂用户的风格，但不需要知道用户的钱袋子。

**问题类型分类**
每个问题会被自动打上三种标签之一：
- **公告类**——需要从财报、公司公告里找具体数字的问题
- **新闻类**——需要从近期新闻里找的问题
- **分析类**——需要从市场研究、行业分析里找的问题

这个分类不是摆设，它直接决定了下一步 Agent B 用什么手段去回答这个问题（见 4.5），是连接"生成问题"和"云端调研"这两个阶段的关键路由信息。

### 4.4 隐私确认闸门：让"看到的等于发出的"从口号变成结构性事实

这道闸门的实现思路是：**用同一份数据对象，既拿来渲染确认界面，又拿来发送**——界面上展示的股票信号、研究问题，和实际发往云端的内容，本质上是同一份数据，不是分别维护两份、靠人工保证一致。这意味着"用户看到的就是要发的"不是一句承诺，而是代码结构本身决定的事实：不可能出现界面显示的和实际发送的不一样的情况，因为它们根本就是同一个东西的两种呈现方式。

**流程真的会停下来，而不是假装停下来**
分析流程执行到确认这一步时，会把当前所有状态——已经完成的本地分析结果、生成好的研究问题——完整保存下来，然后**真正挂起**，不占用任何计算资源空等，用户可以隔几秒点，也可以放上几分钟再回来处理，流程状态原样等在那里。用户做出决定后，流程从**刚才挂起的那个点**继续往下走，而不是从头重新跑一遍——这保证了"确认"这个动作是轻量、即时的，不会让用户因为要等待重新计算而失去耐心。

**确认清单只展示"将要发出去的东西"**
界面上逐股展示：技术信号摘要、估值信息、以及打了类型标签的研究问题清单，**唯独不出现持仓数量和成本价**——这两个字段从确认清单的数据结构里就被排除在外，不是"藏起来不显示"，而是"这份要发的数据里本来就没有它们"，从根源上避免"我看到界面上有,是不是也会被传出去"的用户疑虑。

**用户拥有真正的否决权**
用户可以一键确认继续，也可以直接取消——取消后流程会走向结束，完全不会发起任何联网请求，用户仍然能看到已经跑完的本地分析结果，不会因为拒绝联网而失去本地这部分产出。这让"只看本地结果、不联网"成为一个和"联网调研"平等的、真正可用的选项，而不是一个做样子的按钮。

### 4.5 Agent B —— 云端调研：结构化数据优先，网络搜索兜底

云端这一步的设计原则是：**能用结构化、免费、稳定的公开数据源解决的，就不动用不确定的网络搜索；只有真正需要"市场观点、行业分析"这类主观信息时，才去搜索开放网络。**

**第一层：结构化数据（主力）**
针对每支股票，独立抓取三类结构化公开信息：
- 个股最新新闻（含标题和摘要）
- 当天的题材/热点归因（这支股票今天为什么涨/跌，市场认为的原因）
- 分析师对未来业绩的一致预期数据

这三路彼此独立，各自失败互不影响，且**全部走零注册的免费公开接口**，不需要任何账号或付费订阅，这也是"结构化数据优先"能成立的前提——如果结构化数据源本身要花钱或要审批权限，这套优先级设计就没有意义了。

**第二层：网络搜索（只用于"分析类"问题）**
只有 4.3 里被标注为"分析类"的问题才会走开放网络搜索，"公告类"和"新闻类"问题因为已经被第一层的结构化数据覆盖，不再重复搜索——这样既避免了信息重复，也控制了对外部搜索服务的调用次数。

搜索本身用了**分层域名白名单**：区分"权威公告类站点"（交易所、证监会）、"财经新闻类站点"、"市场观点类站点"三层信源，按问题类型匹配对应层级的白名单，尽量只从信噪比高的站点取材，避免被营销号、标题党内容污染。如果限定白名单搜不到结果，会自动放开限制、扩大时间范围再搜一次，保证"宁可信源杂一点，也不要什么都搜不到"。

**第三层：大模型二次过滤**
搜索引擎返回的原始片段，还会再交给大模型判断一遍——"这段内容是否真的包含能回答这个问题的具体信息，还是只是泛泛的背景介绍"，只留下真正相关的片段。这一步是为了解决搜索引擎"关键词匹配到了，但内容其实答非所问"的通病，如果这一步本身出错，会退回展示未过滤的原始结果，保证不会因为过滤器故障而丢失全部信息。

**隐私约束贯穿始终**
Agent B 从头到尾拿到的输入只有"股票代码 + 脱敏后的研究问题"，搜索请求里从不会拼接持仓量、成本这些字段——这不是靠代码写的时候"记得别传"去保证的，而是因为 Agent B 能接触到的数据对象，从产生的那一刻起结构上就不包含这些字段，Agent B 想传也传不出去。

### 4.6 Fusion —— 本地 AI 融合分析：唯一"个人数据与 AI 相遇"的地方

云端调研完成、结果传回本地之后，才真正进入这套系统里**唯一允许个人数据和 AI 同时出现在同一次计算里**的环节。

这一步会把三样东西汇总喂给本地 AI：
- 云端返回的结构化数据和网络搜索摘要（公开信息）
- 本地算出来的技术信号（趋势、量能、RSI、MACD、KDJ 等）
- **本地的持仓量、成本价，以及现算出来的浮盈/浮亏比例和金额**

浮盈浮亏是本地当场算出来的（现价和成本价一比就知道），这个计算结果只用来构建这一步的分析材料，不会被单独传输或落地到其他地方。最终由 AI 针对每一支股票给出：一句话综合判断、具体操作建议（持有/减仓/加仓/清仓/观望）、一条主要风险提示，并且**明确要求建议要结合当前浮盈浮亏状态**——浮亏的股票要不要补仓、浮盈的股票要不要止盈，这类真正"因人而异"的建议，正是需要知道持仓成本才能给出的，也正因为如此，它被严格限定只能在本地发生。

当前 Demo 阶段，这一步临时借用了云端的大模型接口来生成建议（本地推理硬件的算力集成还在进行中），但**这只是"用哪个模型算"的实现细节，不影响"个人数据在哪里被使用"这个隐私边界**——持仓成本这类数据从始至终只在本地这台机器上被组装进最终发给模型的那段文字里，调研结果是云端传回本地之后、在本地完成的组装，不存在"个人数据被发到云端"这一步。等本地推理硬件就位后，这一步换成本地模型，就是纯粹的算力位置切换，架构完全不用改。

### 4.7 前后端实时体验：把"本地/云端"的边界做成用户肉眼可见的东西

这是我认为除了闸门机制之外，第二个值得单独拿出来讲的结构性设计——**不满足于"文字上说清楚数据在哪计算"，而是把整个分析过程做成一张实时生长的时序图**，左边一列是"本地"，右边一列是"云端"，每一步分析产生的动作都会实时冒出一条对应颜色的消息，本地的信号处理、技术指标计算全部出现在左边（青色），云端的新闻检索、网络搜索全部出现在右边（琥珀色），中间用一条跨越两栏的"发送 →"箭头行，明确标出"数据在这一刻、从这里、跨过了本地和云端的边界"，调研结束后再有一条"← 已返回本地"的箭头行，把边界跨越的那两个具体时刻钉在画面上，用户不需要懂技术，光看颜色和箭头就能确认：贵重的东西（持仓、成本）只在左边出现过。

**渐进式展开，默认克制、按需深入**
搜索结果和结构化数据这些内容量比较大的节点，默认只显示一行摘要（比如"找到 3 条相关信息"），点开才展开成完整内容，避免时序图被信息量过大的内容撑爆，同时又保留了"用户想深挖就能挖到底"的可审计性——这套一体机face向的是可能会被客户、审计人员反复盘问细节的场景，所以"看得到摘要，也点得开原文"这个分寸感是刻意设计的。

**闸门本身可折叠**
研究清单确认之后会自动收起，避免占用界面空间，但用户随时可以重新展开回看当时确认过的内容——确认这个动作发生过什么、当时看到的是什么，不会随着流程推进而消失。

**支持单只或多只联合分析**
用户可以从持仓面板里勾选一支或多支股票同时分析，系统会为每一支单独走完整套本地分析和问题生成流程，最后在综合建议阶段把多支股票的判断汇总在一起呈现——这个设计是为了让 Demo 既能演示"单只深度分析"，也能演示"一次性体检整个持仓组合"这两种典型使用场景，不需要为两种场景做两套代码。

### 4.8 分阶段实时通道：解决"用户确认"这个不确定等待的工程问题

前面提到，闸门会让流程真正停下来等待用户点击，这个等待时间是不可预测的。如果想用一条实时通道从头播到尾，就必须让这条连接在用户思考、倒水、甚至走开的这段时间里也一直占用着——这既不稳定（网络中间设备通常会把长时间没有数据的连接判定为失效并主动掐断），也没有必要。

我的做法是把整个体验拆成**两段独立的实时通道**，中间用一个断点接续：
- 第一段通道只负责播报"本地分析"这一阶段的进度，播报到闸门弹出、等待用户确认为止，然后主动关闭；
- 用户点击确认或取消后，后台立刻在一个新的执行轨道上继续跑后续流程，同时开启**第二段独立的通道**，专门播报"云端调研 + 本地融合"这一阶段的进度。

两段通道之间，靠一个后台生成的会话标识连接起来，确保用户点击确认这个动作，能够准确对接到"当时被暂停的那次分析"，而不会和其他人、其他次分析的状态串在一起。这套设计的价值在于：**用户可以毫无压力地花任意长时间做确认决策**，不用担心"想太久连接就断了"，工程实现上也不需要用轮询这种效率较低的方式去等待一个不确定的用户输入。

### 4.9 工程可靠性与部署细节

- **网络环境隔离**：本机（尤其是 macOS）的系统代理设置，会干扰程序直连行情数据源和私有协议服务，因此在引入这些依赖之前，先显式清空了各类代理环境变量、并安装了一个"不使用任何代理"的网络请求策略，保证不管部署在本地还是云服务器上，网络行为都是可预期、一致的。
- **私有协议连接的长期稳定性**：走私有 socket 协议的历史行情数据源，设计上是"登录一次、长期复用一条连接"，但部署成常年运行的云端服务后，这条连接可能会在闲置一段时间后被对端或网络中间设备悄悄断开，而这类库本身不会自动重连。针对这一点补充了"查询失败时自动重新登录、再试一次"的兜底逻辑，避免云端部署运行一段时间后，行情数据无声无息地全部退化成模拟数据。
- **密钥管理从"内嵌兜底"收紧为"强制配置"**：早期为了方便内部联调，代码里内嵌过一份兜底密钥，即使没配置环境变量也能跑起来；现在已经改为**必须通过环境变量配置密钥，缺失时启动阶段就直接报错阻止启动**，不再允许用一份写死在代码里的密钥悄悄顶上——这避免了"密钥其实没配对，但系统看起来一切正常"这种更危险的隐患。
- **持仓数据可配置化**：持仓清单是一份用户自己可维护的本地文件，不是写死在代码里的示例数据，方便后续接入真实持仓、或者给不同客户演示不同的持仓组合。
- **完成云平台部署**：整套系统已经跑通云端部署，验证了这套"本地分析 + 云端调研 + 本地融合"的架构，在真实的公网环境下也能稳定工作，不只是本机 Demo。

---

## 五、隐私保护特点总结（本项目的灵魂）

| 数据类型 | 本地分析 | 发往云端 | 本地 AI 分析 |
|----------|:---:|:---:|:---:|
| 股票代码 / 名称 | ✅ | ✅ | ✅ |
| **持仓量（qty）** | ✅ | **❌ 从不** | ✅ |
| **成本价（cost）** | ✅ | **❌ 从不** | ✅ |
| 技术指标信号 | ✅ | ❌ | ✅ |
| 研究问题（脱敏） | 本地生成 | ✅ | ✅ |
| 风险偏好 / 备注 | ✅ | ❌ | ✅ |

**四个可审计的隐私保障：**

1. **结构性隔离**：发往云端的数据对象里，敏感字段在代码层面就不存在，不是"记得别传"，而是"根本没有这条传输路径"
2. **用户知情确认**：闸门把发送内容 100% 摊开，用户看到什么就发送什么，而且流程会真的停下来等，不是走个过场
3. **数据最小化**：云端只拿到搜公开新闻所必需的最少信息，能用免费结构化数据解决的绝不动用网络搜索
4. **本地融合**：需要结合个人数据的那一步分析，物理上发生在本地，是唯一一处个人数据与 AI 相遇的地方

---

## 六、为什么这一切都指向"本地推理硬件"

前面所有设计，最终收敛到一个价值主张：

> **只有当最终的 AI 推理在本地进行，"隐私"才是完整的。**

**云端 AI 的根本困境**：要做出真正个性化的建议（"你成本 128，现价 74，浮亏 42%，该不该补仓"），AI 就必须知道你的持仓成本。可一旦知道，数据就上了云。个性化与隐私，在云端架构下是**不可调和**的矛盾。

**本地推理硬件是唯一的解**：

- 云端 AI 负责它该负责的——搜集**公开信息**（新闻、公告、研报），这些本来就是公开的，上不上云无所谓
- 本地 AI 负责它该负责的——把公开信息与**你的私人数据**结合，做出只属于你的判断
- 私人数据与 AI 的"相遇"，发生在你手里的那台机器上，一个字节都不外流

**本项目就是这个价值主张的活体证明。** 当前 Demo 中，融合分析这一步临时调用了云端 API（因为本地模型尚在集成），但整套架构已经为本地模型预留好了位置——**换上本地推理芯片，个人数据就实现了 100% 不出本地的闭环**。届时：

- 用户得到的分析质量，与用云端大模型**没有区别**
- 但用户的持仓、成本、盈亏，**没有任何第三方能看到**

这就是我们的硬件要卖的东西：**不牺牲 AI 能力的数据主权。**

---

## 七、结语

我在本项目中完成的，不只是一个炒股分析工具，而是一套**可以直接向客户演示、经得起技术审计的隐私架构范式**。它把"本地推理硬件为什么值得买"这个抽象的商业主张，变成了一条用户能亲眼看见、工程师能逐行验证的数据流——从持仓文件的字段设计，到问题生成时"个性化提前到发送之前"的取舍，到闸门"看到即发送"的结构性保证，到分阶段实时通道对"不确定等待"这个工程难题的处理，每一处细节都在服务同一个目的：让隐私保护不是一句承诺，而是一连串可以拆开来一步步验证的具体设计。

对潜在客户，它回答了那个最关键的问题：**"我的钱袋子，凭什么交给一台机器？"** —— 因为在这台机器上，AI 为你服务，但你的秘密只有你自己知道。
