1. 浏览器里面加上：

三个接口串起来看

浏览器                          FastAPI                    LangGraph
  │                               │                           │
  │── GET / ──────────────────→  发 index.html               │
  │                               │                           │
  │── POST /research/start ────→  invoke({}) ───────────────→ 跑到 interrupt() 暂停
  │← { thread_id, preview } ────  ←─────────────────────────  返回暂停数据
  │                               │                           │
  │  （用户看确认框，做决定）      │                           │
  │                               │                           │
  │── POST /confirm/{thread_id} → invoke(Command(resume)) ──→ 从暂停点继续跑完
  │← { status, fusion_result } ←  ←─────────────────────────  返回最终结果

  每一步都显示到了哪一步了。

2. 